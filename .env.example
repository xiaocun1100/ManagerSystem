# 后台管理系统配置

## 环境配置

项目支持多环境配置，通过 `.env` 文件管理不同环境的配置。

### 环境变量文件

- `.env.development` - 开发环境
- `.env.production` - 生产环境
- `.env.local` - 本地覆盖配置（不会被 git 跟踪）

### 配置项说明

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| `VITE_API_BASE_URL` | API 基础地址 | `/api` |

### 配置示例

**开发环境 (.env.development)**
```bash
VITE_API_BASE_URL=http://localhost:3000
```

**生产环境 (.env.production)**
```bash
VITE_API_BASE_URL=https://api.example.com
```

### 切换环境

Vite 会根据 `NODE_ENV` 自动选择环境：
- 开发模式：`npm run dev` 使用 `.env.development`
- 生产模式：`npm run build` 使用 `.env.production`

### 本地覆盖

如果需要本地调试，可以创建 `.env.local` 文件：
```bash
VITE_API_BASE_URL=http://192.168.1.100:3000
```

> 注意：`.env.local` 不会提交到 git，适合本地调试使用。
